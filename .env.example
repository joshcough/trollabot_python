# Bot Configuration
BOT_TOKEN=oauth:your_twitch_oauth_token_here

# Database Configuration
# For production, set ENV=prod and provide DATABASE_URL
ENV=dev
DATABASE_URL=postgresql://user:password@host:port/database

# Discord Notifications (optional)
# Create a webhook in your Discord server settings
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Email Notifications (optional)
# All email settings must be provided for email notifications to work

# Gmail Example:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your.email@gmail.com
# SMTP_PASSWORD=your_app_password  # Use App Password, not regular password
# NOTIFICATION_EMAIL=where.to.send@gmail.com

# Generic SMTP Example:
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your.username
SMTP_PASSWORD=your.password
NOTIFICATION_EMAIL=admin@example.com

# Note: If neither Discord nor Email are configured, errors will only be logged to stdout
