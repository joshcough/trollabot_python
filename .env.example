# Bot Configuration
BOT_TOKEN=oauth:your_twitch_oauth_token_here

# Database Configuration
# For production, set ENV=prod and provide DATABASE_URL
ENV=dev
DATABASE_URL=postgresql://user:password@host:port/database

# Discord Notifications (optional but recommended)
# Create a webhook in your Discord server:
# Server Settings → Integrations → Webhooks → New Webhook
# Choose a channel and copy the webhook URL
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# If DISCORD_WEBHOOK_URL is not set, errors will only be logged to stdout/Papertrail
